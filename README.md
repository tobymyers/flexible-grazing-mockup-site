# Riparian exclusion mockup (demo app)

Deployed field-test build. The source of truth (spec, pipeline, notes) lives in the
private `flexible-grazing-mockup` repo. The basemap key is injected at deploy time
from the `ARCGIS_KEY` Actions secret — it is never committed here.
